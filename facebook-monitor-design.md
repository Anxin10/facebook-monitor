# Facebook 指定粉專貼文與限時動態輕量化監控設計

版本：1.0  
日期：2026-09-18  
狀態：設計規格，尚未實作或啟動監控。

## 1. 目標與範圍

監控指定 Facebook 粉專自己發布的公開貼文與限時動態，發現新內容後通知使用者。採用低資源、低維護成本的架構，避免重複通知，並能辨識監控失敗。

| 項目 | 設定 |
| --- | --- |
| 初始目標 | `100054441671873` |
| 目標網址 | https://www.facebook.com/profile.php?id=100054441671873 |
| 帳號類型 | 依使用者指示，預設為粉絲專頁；尚未實際驗證 |
| 監控內容 | 指定粉專自己發布的公開貼文，以及可合法存取的限時動態 |
| 排除內容 | 訪客貼文、留言、私密內容與未獲存取權的內容 |
| 通知管道 | 待選：LINE、Telegram、Email 或電腦通知 |
| 執行環境 | 待選：Windows、NAS 或雲端主機；須持續執行才能定時監控 |

## 2. 可行性與前提

### 2.1 公開貼文

依本次對話中使用者提供的 Meta「Posts」文件，可使用 `GET /page_id/feed` 取得粉專貼文。讀取非自己管理的粉專，需符合文件所述的 User access token 與 `Page Public Content Access` 等存取條件，並完成所需審核。

公開可見不代表應用程式已取得 API 存取權。正式開發前，必須以目標粉專和實際授權驗證讀取能力。只做監控時，應依讀取用途確認必要權限，不直接申請文件中所有發文、修改與刪除權限。

定期讀取公開貼文與訂閱 Webhook 是不同能力。本方案不假設能訂閱任意第三方粉專的 Webhook；若未來取得粉專授權，可另評估 Webhook。

### 2.2 限時動態

使用者提供的 `/feed` 文件未涵蓋限時動態，不能假設該端點會回傳限動。

限動必須設置獨立讀取模組，先驗證官方介面是否可在實際權限下支援。若無法支援，再評估以 Playwright 檢查使用者正常可見的限動；不繞過登入驗證、存取限制或驗證碼。

瀏覽器方案仍受登入狀態、可見範圍及頁面改版影響。需能取得每則限動的穩定識別碼或經驗證的識別方式；只觀察頭像外圈不足以辨識新增的每一則限動。

限動在完成實測前，標記為「待驗證」，不宣稱已正式支援。兩次檢查之間即被刪除、或停機期間已消失的內容可能無法取得，因此不承諾零漏報。

## 3. 輕量化架構

採用 Python 單一常駐程式搭配 SQLite。API 路線不啟動瀏覽器；只有需要瀏覽器讀取且已通過驗證的來源才啟用 Playwright。

| 模組 | 職責 |
| --- | --- |
| 設定載入 | 管理粉專清單、檢查間隔與通知管道 |
| 排程器 | 依來源分別排程，避免同一目標的檢查重疊 |
| 貼文讀取器 | 呼叫官方 API、處理分頁與作者篩選 |
| 限動讀取器 | 經驗證後啟用；與貼文讀取器分離 |
| 正規化與去重 | 統一資料欄位，以內容識別碼去重 |
| SQLite | 保存內容、檢查狀態及待通知項目 |
| 通知發送器 | 發送新內容提醒，失敗時保留待重試項目 |
| 健康狀態 | 區分無更新、存取失敗、限流及登入失效 |

第一版不建置管理網頁、不加入 AI 辨識，也不預設下載圖片或影片。若使用瀏覽器，共用一個瀏覽器實例並依序檢查，避免每個粉專各開一個瀏覽器。

## 4. 初始排程

| 內容 | 初始檢查間隔 | 說明 |
| --- | --- | --- |
| 公開貼文 | 10 分鐘 | 依實際配額、粉專數及錯誤率調整 |
| 限時動態 | 5 分鐘 | 只在讀取方式驗證通過後啟用 |
| 失敗重試 | 逐步延長間隔 | 尊重服務回傳的重試提示，避免持續密集請求 |

上述間隔為設計起點，不是平台保證可接受的頻率，也不是通知延遲保證。限流、網路失敗與停機均可能增加延遲。

## 5. 檢查與通知流程

1. 載入目標設定及既有檢查狀態。
2. 首次成功讀取時建立基準，預設不通知既有內容。貼文與限動分別建立基準。
3. 後續定期讀取內容，核對作者是否為指定粉專；無法判定作者時記錄待確認，不直接視為符合。
4. 處理分頁，採用重疊時間區間並比對既有 ID；不僅依第一頁、置頂位置或顯示順序判斷新舊。
5. 以「粉專 ID＋內容類型＋內容 ID」建立唯一鍵。
6. 在同一資料庫交易中保存新內容及待通知項目，避免只存了內容卻漏建通知。
7. 發送通知；成功後更新狀態，失敗則保留並安排重試。
8. 只有完整成功的檢查才推進成功檢查紀錄；部分讀取失敗須留下狀態，供後續補查。

舊貼文編輯預設不算新發文。重啟程式後沿用資料庫基準及待通知項目，不重新通知所有舊內容。

若通知服務已收件但回應遺失，重試仍可能造成重複通知；應盡可能使用通知管道支援的冪等機制，不宣稱跨服務的絕對一次送達。

## 6. 最小資料模型

| 資料表 | 主要欄位 | 用途 |
| --- | --- | --- |
| targets | page_id、url、enabled | 監控對象 |
| items | page_id、content_type、content_id、author_id、published_at、first_seen_at、summary、url | 內容及去重資料 |
| checks | page_id、content_type、baseline_ready、last_attempt_at、last_success_at、status、error_code | 各來源檢查狀態 |
| notifications | item_key、channel、status、attempts、next_retry_at、sent_at | 待通知與重試紀錄 |

`items` 的唯一鍵為 `(page_id, content_type, content_id)`。通知另以內容與管道建立唯一約束，避免重複建立同一管道的待發送項目。

發布時間未知時保持空值，不以發現時間冒充。內部時間使用 UTC；通知顯示可使用 `Asia/Taipei`。

## 7. 設定示意

以下 YAML 僅為規格示意，尚非可執行程式設定：

```yaml
targets:
  - page_id: "100054441671873"
    url: "https://www.facebook.com/profile.php?id=100054441671873"
    account_type: "page_assumed"
    enabled: true

posts:
  source: "graph_api"
  interval_seconds: 600
  author_filter: "target_page_only"

stories:
  enabled: false  # 完成實測後才啟用
  source: "pending_validation"
  interval_seconds: 300

storage:
  database: "monitor.sqlite3"
  download_media: false

notifications:
  channel: "pending_selection"
  notify_existing_on_first_run: false
  notify_post_edits: false
  timezone: "Asia/Taipei"
```

API Token、通知憑證與瀏覽器登入狀態不寫入公開設定或日誌，應由執行環境的秘密設定或受保護檔案載入。

## 8. 通知格式

每則通知包含：

- 粉專名稱；無法取得時顯示粉專 ID。
- 內容類型：貼文或限時動態。
- 發布時間（若可取得）與首次發現時間。
- 簡短摘要；無文字時標示圖片或影片內容，不強制辨識影音。
- 可取得的原始連結；限動連結可能失效。

## 9. 異常處理

| 狀況 | 系統行為 |
| --- | --- |
| 成功但無新內容 | 更新成功檢查時間，不發新內容通知 |
| Token 過期或權限不足 | 標記授權異常，提醒處理；不當作無更新 |
| 瀏覽器登入失效或遇到驗證 | 暫停相關來源並提醒使用者處理 |
| 限流 | 降低頻率並依提示等待重試 |
| 網路或服務暫時失敗 | 有上限地重試，保留上次成功狀態 |
| 頁面結構改變或解析失敗 | 標記來源不可用，避免錯誤判定無新內容 |
| 分頁未完成 | 記錄不完整檢查，不推進完整成功紀錄 |
| 通知發送失敗 | 保留待通知項目並重試 |

相同異常提醒應合併或設定冷卻時間，恢復後可發一次恢復通知。

## 10. 實作與驗收順序

### 階段一：驗證資料來源

- 驗證目標實際為粉專及其可用 ID。
- 確認 Meta App、存取審核及 Token 狀態。
- 實測取得貼文並確認能可靠篩選指定作者。
- 獨立驗證限動讀取及每則限動識別能力。

### 階段二：完成貼文監控

- 實作排程、分頁、作者篩選、去重、SQLite 與通知重試。
- 首次建立基準不通知舊文。
- 已觀察到的新貼文可建立通知，重啟不重建相同通知。
- 訪客貼文不觸發指定發文者通知。
- 權限錯誤能被辨識，不顯示為「沒有新內容」。

### 階段三：啟用限動監控

- 僅在資料來源驗證通過後啟用。
- 能辨識同一粉專連續新增的多則限動。
- 能區分沒有可見限動與讀取失敗。
- 登入失效能提醒，已過期或未取得的限動不偽稱已補抓。

## 11. 尚待決定

1. 是否已有通過相關審核的 Meta App 與可用 Token。
2. 通知管道及接收設定。
3. 持續執行的設備或主機。
4. 可接受的延遲、監控粉專數量與資料保留期間。
5. 限動讀取方案的實測結果。

以上不影響設計文件成立，但影響實際部署與功能承諾。

## 12. 文件依據

本規格整理自本次對話與使用者提供的 Meta「Posts」文件，未完成目標粉專的實際 API 或限動讀取驗證。官方介面與權限應於實作時依當時文件再次確認。

- [Meta Pages API](https://developers.facebook.com/documentation/pages-api)
- [Page Feed Reference](https://developers.facebook.com/docs/graph-api/reference/page/feed)
- [Page Public Content Access 參考](https://developers.facebook.com/docs/apps/review/feature#reference-PAGES_ACCESS)
- [Webhooks for Pages](https://github.com/Anxin10/facebook-monitor)
