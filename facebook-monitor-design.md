# Facebook 瀏覽器貼文監控架構

版本：2.0

日期：2026-09-20
狀態：瀏覽器輪詢版本已實作；事件觸發最佳化尚未實作；真實 Facebook 頁面與通知收訊待實機驗收。

## 1. 產品定位與執行前提

目前提供「使用者瀏覽指定粉專時，對首次看見的可辨識貼文發送通知」。此版本不提供關閉 Facebook 後仍能持續接收新發文的全天候監控。

運行時必須同時滿足：

- 電腦保持喚醒，瀏覽器與擴充功能可執行。
- 要觀察的指定粉專分頁保持開啟，並維持正常可存取的登入狀態。
- 本機 Python 接收器及通知排程器持續運行。
- Facebook 已將內容載入該分頁，且內容符合目前 parser 支援的結構與連結格式。

不必一直盯著畫面，但開著分頁不代表 Facebook 一定會自動載入新貼文。使用者可能仍需重新整理或捲動。分頁休眠、被凍結或關閉時，無法持續偵測。

## 2. 資料流與模組責任

```text
Facebook 將貼文載入指定粉專分頁
  → content.js 每 15 秒掃描 DOM
  → parser.js 辨識目標貼文連結與摘要
  → background.js 驗證傳送分頁並附上配對碼
  → 127.0.0.1:8765 本機接收器
  → SQLite 單一交易：去重、保存貼文、依啟用狀態建立通知
  → Scheduler 每 30 秒處理待發通知
  → Apprise／LINE
```

| 模組 | 責任 |
| --- | --- |
| `extension/content.js` | 掃描目前已載入的頁面；同一時間只執行一次掃描 |
| `extension/parser.js` | 正規化 permalink，排除非指定目標與不支援的連結；擷取可辨識摘要 |
| `extension/background.js` | 根據訊息傳送者的頁面核對目標，向固定的本機端點傳送資料 |
| `extension/popup.js` | 配對、顯示觀察狀態，明確啟用／暫停通知 |
| `src/browser_receiver.py` | 認證、輸入與目標驗證、基準狀態、交易式入庫 |
| `src/database.py` | 內容唯一鍵、通知佇列與重試狀態 |
| `src/scheduler.py` | 處理通知排程；不抓取 Facebook 頁面 |
| `src/notifier.py` | 依通知管道發送與重試 |

主程式設定為 `posts.source: browser`。原有 Graph API 程式僅供獨立診斷，不是瀏覽器來源的自動備援。本架構沒有 Facebook 新貼文訂閱、Webhook、自動重新整理、自動捲動或背景抓取。

## 3. 延遲的定義

延遲必須分開計算：

1. **發布到載入分頁**：目前不可控制，也沒有時間上限。分頁未取得貼文，就無法偵測。
2. **載入後到開始發送**：正常、未受節流且無積壓／失敗的排程下，15 秒掃描加 30 秒通知輪詢，約 0～45 秒的排程等待，再加上解析、處理與網路時間。
3. **通知平台接受到使用者收到**：由通知平台與接收端狀態決定；失敗重試會增加延遲。

0～45 秒不是發布後的送達承諾，也不是最長延遲保證。分頁休眠、系統負載、通知佇列積壓與服務故障都可能增加等待。

## 4. 基準、去重與通知語意

初次觀察先記錄基準，不發通知。至少觀察到一篇可辨識貼文後，使用者才能在 popup 按「基準確認，開始通知」。

啟用後，對首次入庫的貼文建立各管道的待發通知。暫停通知期間仍記錄貼文，恢復後不補發這些內容。基準、啟用狀態與去重資料在重啟後保留。

唯一鍵為 `(page_id, content_type, content_id)`，各通知再以管道 ID 去重。貼文與通知建立放在同一 SQLite 交易內，避免只寫入內容卻漏建通知。

「首次看見」不等於「剛發布」：第一次捲到舊文也可能觸發通知。訊息標示「瀏覽器首次看見；非發文時間」，未知的發布時間保持空值。回看歷史內容前可先暫停通知。

## 5. 資料與安全邊界

- `items`：貼文 ID、摘要、原始連結、首次入庫時間；來源標記為 browser。
- `browser_state`：每個目標的通知啟用狀態、最近收到有效非空觀察的時間、已記錄篇數。
- `notifications`：每個管道獨立的待發、成功、失敗與重試資料。
- 本機接收器固定綁定 `127.0.0.1:8765`，要求配對碼並驗證 Host／Origin。
- 配對碼存於 `browser-token.local` 及擴充功能的 trusted storage，不傳入 Facebook 頁面。
- 不收集 Facebook Cookie 或 Access Token；通知憑證留在本機環境設定。

popup 的「本機服務已連線」只證明接收器可回應，不證明 Facebook 正在載入新貼文。「最近收到」可能由重複觀察更新，也不代表已完整檢查粉專。

## 6. 中斷與恢復

| 狀況 | 目前行為 |
| --- | --- |
| 分頁關閉／休眠、電腦休眠 | 停止或延後觀察，沒有完整補抓保證 |
| 分頁開著但 Facebook 沒有載入新貼文 | 無法偵測；可能需使用者重新整理 |
| 登入失效、版面變更或沒有可辨識貼文 | 不入庫；目前不能自動區分這些原因與真正沒有內容 |
| 本機接收器暫時不可用 | 已待送的批次保留在該分頁記憶體，後續嘗試重送 |
| 關閉／重新載入／導向分頁 | 尚未確認入庫的記憶體批次可能遺失 |
| Python 服務停止 | 不接收新觀察，也不處理通知；已入庫待發資料留在 SQLite |
| 通知發送失敗 | 沿用既有各管道重試機制；不可重試錯誤保持失敗狀態 |

通知服務接受訊息後若回應遺失，仍可能重複送達；不承諾跨平台絕對一次送達。單一 Python 實例運行，不同時啟動多個實例共用同一資料庫。

## 7. 事件觸發最佳化方向（尚未實作）

可將定期完整掃描改為：觀察 DOM 新增／變更 → 合併短時間內的變動 → 只解析受影響的貼文區塊。另可在入庫後喚醒通知器，以縮短通知輪詢等待。

這只改善「內容已載入」之後的解析成本與處理延遲，仍然需要指定粉專分頁持續開啟且可執行；不會使 Facebook 自動取得新貼文，也不等於向 Facebook 訂閱發布事件。

若實作，需涵蓋初始掃描、SPA 換頁、同一貼文的分批載入、節流、去重與重送測試。完成實作及驗證前，不將此能力標示為已提供。

## 8. 範圍與驗收

支援桌面版 `www.facebook.com` 指定粉專首頁，以及可核對目標的 `/帳號/posts/ID`、`permalink.php`、`story.php`。Reels、社團、限時動態與獨立影片頁不在支援範圍；不保證全文、完整歷史或零漏報。

離線驗證覆蓋來源驗證、基準、重啟去重、交易回滾與通知流程。實機驗收必須另外確認：

- 指定粉專實際 DOM 可辨識，既有內容建立基準時不通知。
- 啟用後首次觀察內容可入庫並送達，重複觀察不重建通知。
- 停止服務／關閉分頁後的狀態符合上述限制。
- 觀察時間與發布時間分開表達，不以分頁連線狀態宣稱全天候監控正常。

若產品需求改成「不用開 Facebook，也能持續收到第三方粉專新發文通知」，需要重新選擇可持續取得內容的資料來源；不能僅靠事件觸發最佳化達成。

安裝、設定與操作詳見 [BROWSER_SETUP.md](BROWSER_SETUP.md)。
