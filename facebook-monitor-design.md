# Facebook 背景貼文監控架構

版本：3.0；日期：2026-09-20。

狀態：背景讀取原型。真實 Facebook 工作階段、通知收訊與長時間資源量測仍待使用者實機驗收。

## 目前規劃與進度

| 階段 | 內容 | 狀態 |
| --- | --- | --- |
| A | 以獨立 Edge 設定檔保存登入狀態；不讀取日常瀏覽器 Cookie，也不把 Cookie 傳給 Python HTTP 服務 | 已實作 |
| B | 以 `login` 指令由使用者手動登入；背景程序只使用 headless 工作階段 | 已實作 |
| C | 每 300 秒依序讀取目標粉專；每輪只建立一個瀏覽器與一個分頁，完成後關閉 | 已實作 |
| D | 解析 permalink、SQLite 去重、首次基準、通知佇列與既有 Telegram／LINE 重試 | 已實作 |
| E | 無主控台啟動、狀態查詢、正常停止、程序鎖、登入失效暫停 | 已實作 |
| F | 本機單元測試、JavaScript parser 測試、合成 DOM 的無頭瀏覽器 smoke test、pythonw 隱藏執行測試 | 已通過 |
| G | 使用實際 Facebook 登入工作階段驗證目標粉專讀取、通知收訊、重複去重 | 待實機驗收 |
| H | 連續數輪量測 CPU、記憶體、檢查耗時與電腦操作影響，設定可接受門檻 | 待實機驗收 |
| I | 若實機不穩定，評估正式資料供應商或重新申請官方 API 存取資格 | 條件式後續 |

目前程式與文件已推送至 GitHub `fix/core-correctness`。CI 只驗證程式、合成頁面與測試環境；CI 綠燈不代表 Meta 線上工作階段或通知管道已驗收。

下一個具體操作是：執行 `main.py login` 完成一次手動登入，執行 `main.py check`，查看 `main.py status` 的 `target:ID.status`、`login_required`、`cycle_seconds` 與 `resources`，再用一個新的可辨識貼文驗證真實通知。遇到 checkpoint、登入失效或無法辨識內容時，保留狀態並停止重試，不自動繞過驗證。

## 需求與邊界

在使用者電腦背景低頻運行，不影響日常瀏覽器，不開可見視窗、不搶焦點；僅透過設定的通知管道傳送貼文通知。使用者不需要常開指定粉專分頁。電腦及 Python 程序必須持續運行，休眠時無法檢查。

可見瀏覽器只允許由使用者主動執行 `login` 指令開啟，用於首次登入或重新驗證。背景模式固定 headless，不在錯誤時改成 headed。登入失效時暫停並記錄狀態，不自動彈出登入、錯誤或驗證視窗。

## 資料流

```text
無視窗啟動器 → Python 主迴圈
  → 每 300 秒開始一輪（上輪完成後計時）
  → 獨立持久設定檔 + 一個無頭 Edge + 一個共用分頁
  → 依序載入設定目標、等待可辨識 DOM
  → permalink 核對、文字摘要
  → SQLite 單一交易：貼文去重、基準／啟用狀態、通知佇列
  → 關閉該輪瀏覽器
  → 處理通知與失敗重試
```

目前採用單一主迴圈，讀取／通知按序執行，不重疊。待發通知每 30 秒嘗試處理，但讀取期間會延後；不以該間隔承諾最大通知延遲。單頁導航 20 秒、等待可辨識內容 15 秒，錯誤留待後續週期處理；登入／存取驗證失敗則暫停。

## 模組

| 模組 | 職責 |
| --- | --- |
| `main.py` | run／check／login／status／stop／enable／pause 入口與主迴圈 |
| `src/background_monitor.py` | 專用設定檔、程序鎖、無頭讀取、登入中斷、健康與資源取樣 |
| `src/post_parser.js` | DOM 內的貼文連結與摘要辨識，不讀 Cookie |
| `src/observation_store.py` | 目標驗證、首次基準、交易式貼文／通知入庫 |
| `src/database.py`、`src/notifier.py` | 既有 SQLite 去重與多管道通知重試 |
| `start-background.vbs`、`stop-background.vbs` | Windows 無主控台啟動與正常停止要求 |

已移除常開粉專分頁的擴充功能、配對碼與本機 HTTP 接收流程。資料直接在 Python 程序內傳遞，不增加本機網路服務。原 Graph API 工具只作獨立診斷，不是自動備援。

## 工作階段與資料

Windows 使用系統已安裝的 Edge，Linux CI 使用 Playwright Chromium。登入資料只在 `.runtime.local/profile` 內由瀏覽器維護，不讀取日常瀏覽器設定檔，不匯出 Cookie 給 HTTP 爬蟲。登入與背景監控共用程序鎖，避免同時使用該設定檔；程序崩潰後 OS 釋放鎖。

SQLite `items`、`browser_state`、`notifications` 繼續保存貼文、基準與通知。新增 `runtime_state` 保存停止要求、登入狀態、目標讀取結果與量測取樣。所有本機敏感資料及瀏覽器二進位檔在 Git 忽略範圍內。

首次成功觀察預設不通知，需使用者明確 enable。pause 只停止建立新通知，仍記錄內容，已排隊通知仍發送。以 `(page_id, content_type, content_id)` 去重；同一交易建立各管道通知。重啟不清除基準。

## 低資源策略與量測

共用一個瀏覽器及分頁、逐一檢查、阻擋圖片／影音／字型下載，每輪結束釋放整個瀏覽器。閒置期間不保留 Facebook 渲染程序；不自動捲動或追趕漏掉的排程。最低間隔 60 秒，預設 300 秒。

狀態記錄程序樹 RSS 總和、累計 CPU 秒數的當下取樣及一輪耗時。取樣不等於峰值，CPU 秒數不等於使用百分比；CPU／記憶體是否輕量需要在真實目標上量測，不承諾固定上限。阻擋資源也可能影響目標頁面可用性，須實機驗證。

## 異常與運行狀態

- checkpoint、登入表單、401／403：記錄 login_required，停止後續自動讀取；人工停止、登入、重啟後再試。
- 429／其他 HTTP 錯誤、導航逾時：記錄 unavailable，下個週期再試，不密集重試。
- 重新導向其他粉專／不支援 URL：失敗，不將其他內容標成原目標。
- 找不到貼文：記錄 no_recognizable_posts，不當成「沒有新貼文」的成功檢查。
- 瀏覽器啟動失敗：只寫狀態，不切換可見模式。
- stop：在主迴圈或目標間檢查停止要求；當前請求／通知需完成或逾時後才釋放資源。
- 通知失敗：沿用可重試錯誤的佇列及退避，不宣稱跨平台絕對一次送達。

## 可用性及驗收

本版本通知的是「首次觀察到的貼文」，未知發布時間保持空值。粉專排序、置頂、延遲載入和只讀取首頁都會影響可見內容，不保證完整歷史或零漏報。5 分鐘是檢查頻率，不是發文後送達保證。

事件觸發 DOM 最佳化不再是主要解法：主動定時載入負責取得內容，DOM parser 負責解析。Headless 工作階段仍可能受到 Facebook 驗證與反自動化限制；不繞過驗證碼或擅自替使用者登入。

驗收分為單元測試、合成 DOM 的真實 Chromium smoke test，以及實際 Facebook／通知／資源測試。前兩項不能代替第三項。操作與故障處理詳見 [BROWSER_SETUP.md](BROWSER_SETUP.md)。
